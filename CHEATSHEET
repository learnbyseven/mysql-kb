PG DEPLOY DOCKER
docker run -d -p 3306:3306 --name mysql --restart always -e MYSQL_ROOT_PASSWORD=root@123 mysql

Install MY SQL client UB
apt-get install mysql-client

Connect MYSQL server
mysql -h192.168.1.50 -uroot -proot@123

create database user;
use use



CREATE TABLE emp (
  ID SERIAL PRIMARY KEY,
  name VARCHAR,
  title VARCHAR,
  age INTEGER,
  department VARCHAR
);
CREATE TABLE product (
  ID SERIAL PRIMARY KEY,
  name VARCHAR(255),
  price INTERGER
);

CREATE TABLE products (name VARCHAR(255), address VARCHAR(255))



Correct //

CREATE TABLE product (   ID SERIAL PRIMARY KEY,   name VARCHAR(255),   price INTERGER, );
